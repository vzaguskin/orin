# Track model files in models/ with Git LFS
models/** filter=lfs diff=lfs merge=lfs -text

# Или, если хочешь точнее — по расширениям:
# models/**/*.onnx filter=lfs diff=lfs merge=lfs -text
# models/**/*.rknn filter=lfs diff=lfs merge=lfs -text
# models/**/*.bin filter=lfs diff=lfs merge=lfs -text
# models/**/*.pt filter=lfs diff=lfs merge=lfs -text
# models/**/*.tflite filter=lfs diff=lfs merge=lfs -text
# models/**/*.pb filter=lfs diff=lfs merge=lfs -text
# models/**/*.param filter=lfs diff=lfs merge=lfs -text
# models/**/*.model filter=lfs diff=lfs merge=lfs -text
# models/**/*.safetensors filter=lfs diff=lfs merge=lfs -text

# Явно исключим скрипты и текстовые файлы из LFS (если вдруг попадут)
*.py -filter -diff -merge text
*.sh -filter -diff -merge text
*.txt -filter -diff -merge text
*.md -filter -diff -merge text
*.json -filter -diff -merge text
*.yaml -filter -diff -merge text
*.yml -filter -diff -merge text
# LFS config marker Mon Sep 15 11:20:30 UTC 2025
